#include "BlackList.h"
#include "Auth.h"
#include <iostream>

using namespace std;

int main()
{

    Auth auth;

    /*
    BlackList List;

    string tmpIp;
    cout << "Enter IP to check: ";
    cin >> tmpIp;

    bool res = List.isBlack(tmpIp);

    if(res == true){
        cout << "That's ip is black" << endl;
    }else{
        cout << "That's ip is not black" << endl;
    }

    */

    return 0;
}